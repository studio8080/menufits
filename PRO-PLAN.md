# PRO-PLAN.md — 料金プランの設計

作成: 2026-09-01。MenuFits に有料プラン（Pro）を導入するための設計書。
**まだ何も実装していない。実装前に本書の「未決事項」を確定させること。**

姉妹プロダクト MiseFits（`C:\Users\chaha\repos\Misefits`）の `HANDOFF.md` / `AGENTS.md` に
決済まわりの先行事例が詳細に記録されている。**流用できるものは流用し、失敗した判断は繰り返さない。**

---

## 0. 決定事項

- **Web版の買い切り ¥1,480 一本で始める。** 月額・年額サブスクは採らない
- **iOSアプリは後回し。** Web版 Pro の実績が出るまで着手しない（理由は §5）
- **無料と Pro の切り分けは、既存機能を含めて白紙から引き直す**（本書の主題）

### 月額を採らない理由

飲食店のメニュー改訂は年2〜4回のイベント駆動で、常時使うツールではない。月額にすると
「必要な月だけ契約して解約する」のが利用者にとって正しい行動になり、LTVが伸びない。
買い切りなら「1回払えば次の改訂でも使える」で、この使い方に素直に合う。

MiseFits も 2026-08-31 にサブスク（クラウド保存 ¥480/月）の提供取りやめを決定している。
理由の1つが「端末をまたぐ同期には本人識別＝アカウント登録が必須で、**登録不要という商品説明と
正面衝突する**」で、これは MenuFits にもそのまま当てはまる。

将来サブスクを足すなら、**継続コストが実際に発生する機能に限る**（クラウド保存、QRで読める
モバイル版メニューのホスティングなど）。判断は買い切りの実績を見てから。

---

## 1. いま切り替えるべき理由（時間的な前提）

| 事実 | 確認元 |
|---|---|
| サイト公開は 2026-08-21（初回リリースコミット `9adc5c0`） | `git log` |
| note記事・SNSでの**告知はまだ一切していない** | `note-assets/リリースノート記事.md` は下書きのまま未公開 |
| 検索・AIクローラーには開放済み（robots全許可・sitemap・llms.txt） | `robots.txt` / `sitemap.xml` / `llms.txt` |
| **利用状況は誰にも分からない**（GA4が未導入・`gtag` の記述ゼロ） | `index.html` |
| 公開ページ上では「無料」を明示している | JSON-LD `isAccessibleForFree:true` / `offers price 0`、FAQ「登録・課金・透かしは一切ありません」（`index.html` 1028行・1079行） |

**告知前に切り替えれば、これは「値上げ」ではなく「最初からそういう商品だった」になる。**
告知してから有料化すると、その落差は永久に付いて回る（MiseFits はまさにその順序で苦労し、
「無料版は一切変更せず Pro は上乗せのみ」という重い縛りを自分に課すことになった）。

### 残る小さなリスクと、その処置

11日間・告知ゼロ・新規ドメインなので既存利用者はほぼ居ないと見てよいが、ゼロとは言い切れない
（GA4が無いので確認手段が無い）。処置は次の2つで足りる。

1. **切り替えは告知より前に行う。** 順序を絶対に逆にしない
2. 「公開初期からお使いいただいていた方へ」として、問い合わせがあれば**ライセンスキーを無償発行する**。
   Firestore の `menufitsLicenses` に1件手で足すだけなので運用コストはほぼゼロ。
   `releases.html` にその旨を1行書いておけば、それ自体が誠実さの表明になる

### 透かしは入れない

出力物に透かしを入れると、無料版で作ったメニューは**店で使えない**。無料版の存在価値が消え、
「登録不要ですぐ作れる」という唯一の集客資産まで一緒に失う。
MiseFits も同じ結論で透かし機能を実装していない。**無料版のPDFは透かしなし・枚数無制限。**

---

## 2. 切り分けの原則

1. **無料だけで「1枚のメニューを最後まで作って印刷できる」。** ここを割ると、
   広告予算ゼロ・SEOと口コミだけが集客経路である以上、入口そのものが枯れる
2. **課金の壁は「価値を感じた後」に置く。** 打ち込む前ではなく、打ち込んだ後に来る欲求
   ——冊子にしたい／作り分けたい／店のブランドに寄せたい——の手前に置く
3. **データは人質にしない。** 自動保存と `.json` の書き出し・読み込みは**必ず無料**。
   「作ったものを取り出せない」は不安を売る形になる
4. **和洋中・カフェ・写真の5系統は無料側に必ず1つずつ置く。** 「うちの業態では使えない」で
   離脱されると、その人は二度と来ない
5. **縦書き（KINARI）は無料。** MenuFits 最大の差別化がここにあり、有料の裏に隠すと
   何が優れたツールなのかが伝わらない

**ひとことでの説明：「1枚は無料。作り込みと作り分けは Pro。」**

---

## 3. 無料 / Pro の切り分け（本案）

### 無料 — 1枚のA4メニューを、写真つきで、最後まで

| 領域 | 内容 |
|---|---|
| 編集 | 紙面の文字を直接打ちかえ／カテゴリ・品目の追加・削除・並べ替え・複製／おすすめバッジ |
| デザイン | **5種** — EDITORIAL（カフェ）／ KINARI（和・縦書き）／ NOIR（ビストロ）／ SHU（中華）／ ALBUM（写真メニュー） |
| 書体 | 和文8種 × 欧文6種（48通り） |
| 素材 | 背景パターン6種／飾り枠3種／アクセント色10色／通貨表記3種 |
| 写真 | 品目写真・ヘッダー写真・ロゴ |
| 文字装飾 | 太字・下線・マーカー・差し色 |
| 紙面 | **2ページまで**／自動レイアウト（段組追加・自動縮小）／はみ出し警告 |
| 履歴 | Undo・Redo |
| 保存 | 自動保存／スロット保存 **1枠**／`.json` 書き出し・読み込み（**無制限**） |
| 出力 | **A4のPDF（透かしなし・無制限）** |

無料5デザインは、LPのサンプル導線（`#cafe` `#washoku` `#bistro` `#chuka`）が
そのまま動く4種＋写真メニューという構成。**「写真が入れられる」ことの証明を無料側に置く**のが狙い。

### MenuFits Pro（買い切り ¥1,480）

| # | 機能 | 中身 | 状態 |
|---|---|---|---|
| 1 | **デザイン全種** | 11種すべて（＋TERRA 定食／BOLD 居酒屋・ラーメン／LUMIERE コース／POP 喫茶／SPLIT ビストロノミー／SPICE エスニック）。**以後の追加分もすべて含む** | 実装済み |
| 2 | **書体・素材フル解放** | 和文21×欧文18（378通り）／背景パターン23種／飾り枠9種／**紙・文字・柄・枠の色を個別に調整**／背景画像 | 実装済み |
| 3 | **冊子にする** | 表紙・裏表紙（4スタイル）／**3ページ以上**（頁+ の上限解除） | 実装済み |
| 4 | **自由に作り込む** | 自由配置ブロック（テキスト・画像）／テンプレート文字のその場ドラッグ移動・拡大縮小 | 実装済み |
| 5 | **作り分けと再利用** | スロット保存 無制限（グランド／ランチ／季節）／品目ライブラリ（☆登録）／店舗プロフィール（系列店・屋号の切替） | 実装済み |
| 6 | **出力の拡張** | 用紙サイズ（A3/B5/A5/長尺ドリンク/卓上三つ折り）／画像で書き出し（PNG・JPG）／一括価格改定／印刷所入稿PDF（塗り足し3mm＋トンボ） | **未実装・後日追加** |

### この案の一番大きな利点：新規開発ゼロで売り始められる

**Pro の①〜⑤はすべて実装済み。** やるのはロック（`isPro()` での分岐）と購入導線だけ。
前案（未実装の新機能だけを Pro にする）では販売開始まで 15〜25日かかったが、本案なら
**新機能を1つも作らずに販売を開始できる。**

⑥は売り始めた後に追加していく。買い切りなので**後から足した機能は既存購入者にも無料で入る**。
これは購入ページに明記する。「今買っても損しない」は買い切り商品の最大の後押しになる。

### ロックのかけ方

- **PDF出力の直前でロックしない。** 打ち込み切った後で止められるのが最悪の体験。
  ロックは**選ぼうとした瞬間**（Proデザインをクリック、Pro書体を選ぶ、表紙ボタンを押す等）に出す
- Pro要素には **PROバッジを常時表示**しておく。押してから初めて有料と分かる作りにしない
- クリックしたら**決済ページへ直行させず、購入前モーダルを挟む**（MiseFits の教訓）。
  触った機能名・¥1,480・買い切りである点・解放される内容一覧を見せてから Stripe へ送る
- Proデザインのモーダルには、**そのデザインで作った紙面のサンプル画像**を載せる。
  （将来の改善候補：自分の入力内容のままプレビューを表示して「このデザインを使う」で購入へ送る。
  こちらの方が確実に強いが、編集を続けさせてから止める形にならないよう設計に注意が要る）
- ページ上限に当たったときは、はみ出し警告と同じ場所で
  「文字を小さくする／2段組みにする／Proで3ページ以上にする」の3択を出す。**行き止まりを作らない**

### 主なロック地点（実装時のチェックリスト）

| 場所 | 対象 |
|---|---|
| `#designs` のチップ | Pro 6デザイン |
| 書体セレクト（`JPF` / `ENF`） | Pro 側の和文13種・欧文12種 |
| `#patChips` / `#decoChips` | Pro 側のパターン17種・飾り枠6種 |
| 色ピッカー（`state.colors` の paper/ink/pat/deco） | 全部 Pro |
| 背景画像（`state.bgImg`） | Pro |
| `#coverStyleChips` と表紙・裏表紙のトグル | Pro |
| カテゴリの「頁+」 | 3ページ目以降で Pro |
| 「＋テキスト」「＋画像」ブロック追加 | Pro |
| `showFieldTool()`（テンプレート文字のドラッグ・拡縮） | Pro |
| スロット保存（`SLOTKEY`） | 2枠目以降で Pro |
| 品目ライブラリ ☆ / 📋（`ILKEY`） | Pro |
| 店舗プロフィール（`SPKEY`） | Pro |

**無料のまま絶対に触らないもの**：自動保存（`KEY`）、`.json` の書き出し・読み込み、
Undo/Redo、A4 PDF出力、文字装飾、品目・ヘッダー・ロゴの写真。

---

## 4. 公開ページの書き換え（切り替えと同時に必須）

現状 MenuFits は静的ファイルだけで、**バックエンドも解析も法定表示も無い**。

### 4-1. 新規に作るページ

| ファイル | 内容 | 必須度 |
|---|---|---|
| `tokushoho.html` | **特定商取引法に基づく表記**。事業者名・所在地・連絡先・価格・支払時期・引渡時期・返品条件 | **法的に必須** |
| `privacy.html` | プライバシーポリシー。現状は `index.html` 内のモーダルのみで独立ページが無い。GA4とStripe決済の記載が要る | **必須** |
| `pro.html` | 料金・Proガイド。**検索やSNSからの着地点なので、このページ単体で購入まで完結させる**（MiseFits の教訓） | 必須 |
| `pro-unlock.html` | 決済後のリダイレクト先。`session_id` をポーリングしてキー表示＋「この端末で解放して開く」ボタン | 必須 |
| `faq.html` | 無料/Proの線引き・デバイス上限5台・返金・買い切りの説明 | 必須 |
| `releases.html` | 更新履歴。Pro機能の追加を告知し続ける場所。公開初期利用者への無償キー案内もここに | 推奨 |

### 4-2. `index.html` の書き換え

> **【重要・公開順序】この節の書き換えは、Pro が実際に買える状態になってから
> （フェーズ3と同時に）本番へ出す。** 先に出すと「¥1,480 の Pro があります」と書いてあるのに
> 買えないサイトになる。同じ理由で、**`sitemap.xml` への `pro.html` / `faq.html` /
> `releases.html` / `tokushoho.html` / `privacy.html` の追加もフェーズ3で行う**
> （それまで各ページはどこからもリンクされず、実質見つからない状態にしておく）。

**構造化データと表示文言は必ず同時に直す**（片方だけだと JSON-LD と本文がずれる）。

- **JSON-LD（1015行〜）**
  - `offers` を無料と有料の2つにする：`[{"@type":"Offer","price":"0","priceCurrency":"JPY"},{"@type":"Offer","name":"MenuFits Pro","price":"1480","priceCurrency":"JPY"}]`
  - `isAccessibleForFree:true` は**残してよい**（無料で使える範囲が実在するため）が、`description` は
    「無料で1枚のメニューが作れる」旨に改める
- **FAQ「本当に無料ですか？」**（JSON-LD 1028行 ＋ 本文 1079行の両方）→ 書き換え案：
  > **1枚のメニューを作って印刷するところまでは、ずっと無料です。** 登録も透かしもありません。
  > デザインの全種類、冊子（表紙・3ページ以上）、複数メニューの作り分けをまとめた
  > 買い切りの「MenuFits Pro（¥1,480）」を別途ご用意しています。月額課金ではありません。
- **meta description（7行）**「登録不要・無料。」→「登録不要。無料で1枚のメニューが作れます。」
- **LPのデザイン一覧** — 11種すべて表示したまま、Pro 6種に PRO バッジを付ける。
  「デザイン11種」という訴求は落とさない（総数として正しい）
- **`llms.txt`** — 「登録不要・無料で使える」→ 無料範囲と Pro の区別を1行で書く
- **ヘルプモーダル（`#helpBtn`）** — 各項目に PRO 表記を追加。
  ※12番の「和文15種×欧文8種」は**現状すでに実際の21×18とずれている**ので、ここで一緒に直す

### 4-3. GA4 の導入（必須）

MenuFits には現在まったく入っていない。課金ファネルの計測に要る。MiseFits と同じ6イベント：

| イベント | 送る場所 | 主なパラメータ |
|---|---|---|
| `pro_lock_hit` | Pro要素に触れた瞬間 | `feature`（`design:<id>` / `font_jp` / `pattern` / `color` / `cover` / `page3` / `block` / `fieldtool` / `slot` / `itemlib` / `shopprofile`） |
| `pro_buy_click` | 決済リンクを開く直前 | `feature`, `location`（`app_modal` / `hero` / `footer`） |
| `pro_detail_click` | 購入モーダルの「詳しく見る」 | `feature` |
| `license_unlock_success` / `license_unlock_fail` | キー検証の成否 | `location`, `reason` |
| `purchase` | `pro-unlock.html` でキーを表示できたとき | `transaction_id`, `value:1480`, `currency:'JPY'` |

`purchase` は `localStorage` の `menufitsPurchaseTracked:<session_id>` で二重計上を防ぐ。
オプトアウトと Do Not Track を尊重するローダーを使う（MiseFits の実装をそのまま移植）。

**この切り分けは仮説にすぎない。** ①〜⑤のどれが実際に財布を開かせるかは、やってみないと分からない。
`pro_lock_hit` の `feature` 内訳と `pro_buy_click` との差が、次にどこを厚くするかを教えてくれる。
**計測を後回しにすると、この設計が当たったのか外れたのか永久に分からなくなる。**

---

## 5. 決済基盤の実装

### 5-1. MiseFits から流用する

`Misefits/functions/index.js` の3関数構成をコピーして使う。

- `stripeWebhook` … `checkout.session.completed` を署名検証（`req.rawBody`）→ キー発行 → Firestore 書き込み → 控えメール送信
- `issueLicense` … `session_id` からキーを返す読み取り専用API
- `verifyLicense` … キー検証＋デバイス登録（1キー最大5台、`devices` 配列に `arrayUnion`）
- `firestore.rules` … クライアントからの直接読み書きを全面拒否。発行・検証は Admin SDK 経由の Functions のみ
- Stripe Payment Links（`{CHECKOUT_SESSION_ID}` 付きで自サイトへリダイレクト）。手数料 3.6% 程度

### 5-2. Firebase は新規に作らず、既存 `misefits` プロジェクトに相乗りする

**推奨：既存プロジェクト `misefits`（Blazeプラン）に MenuFits 用の関数を追加する。
稼働中で売上が立っている既存3関数には手を触れない。**

理由：`Misefits/HANDOFF.md` の「GCP環境構築でハマった点」に4件が記録されている。
(1) Cloud Build サービスアカウントへの `roles/cloudbuild.builds.builder` 付与、
(2) Compute Engine API の有効化、
(3) 組織ポリシー `constraints/iam.allowedPolicyMemberDomains` の例外設定（`allUsers` 公開が
ブロックされる。組織レベルの `roles/orgpolicy.policyAdmin` が必要）、
(4) 実行SAへの `roles/datastore.user` 付与。
**新規プロジェクトを作ると、この4つを全部やり直すことになる。**
いずれも「デプロイは成功するのに動かない」形で現れるので、避けられるなら避ける。

グローバル `CLAUDE.md` の「`studio@` の Firebase に存在するのは `kininarumono-techo` と `misefits`
の2件だけでよい」という方針とも整合する（**ただし「MenuFits に Firebase は要らない」の記述は
実装着手時に更新すること**）。

相乗りの具体：

- 関数名を分ける（`menufitsStripeWebhook` / `menufitsIssueLicense` / `menufitsVerifyLicense`）
- Firestore のコレクションも分ける（`menufitsLicenses` / `menufitsSessions`）
- キー接頭辞は `MNPRO-`（MiseFits の `MFPRO-` と紛らわしいので `MFPRO` は使わない）
- CORS の `ALLOWED_ORIGIN` は `https://menufits.kokokikaku.com`
- Stripe の Webhook エンドポイントも別に作る。**署名シークレットはテストと本番で別物**なので取り違えない
- Payment Link 作成時、既定でONの「Managed Payments」（取引あたり3.5%の追加手数料）と
  電話番号収集は **OFF にする**（MiseFits で踏んだ）
- 解放UIは左パネルに `#webProSection`。`isPro()` は `localStorage` の `menufitsWebLicense` を見るだけ。
  デバイスIDは `menufitsDeviceId` に永続化

デメリットは請求とログが1プロジェクトに混ざること。Cloud Run のサービス名で切り分けられるので許容する。

---

## 6. iOSアプリを後回しにする理由

MiseFits も 2026-08-28 に iOS リリース中止を決定しているが、**MenuFits には MiseFits に無かった
技術的障壁が3つある。**

### 障壁1：PDF出力が `window.print()` に依存している（最大の問題）

MenuFits の PDF 出力は jsPDF ではなく**ブラウザの印刷機能そのもの**
（`@page{size:A4;margin:0}` ＋ `window.print()`、`index.html` 994行 / 2574行）。
WKWebView では `window.print()` が期待通りに動かず、**Expo の WebView に包むとこのボタンが無反応になる。**

つまり **MenuFits の看板機能（「ひらいて、打ちかえて、PDF」の PDF の部分）がアプリでは動かない。**
対処は次のどちらかで、どちらも小さくない。

- ネイティブ側の `expo-print` に HTML を渡して出力させる橋を作る
  → `@page` / `break-after:page` / Google Fonts の再現性を1から検証し直す必要がある
- html2canvas + jsPDF 系に作り替える → 出力品質が落ちる（テキストがラスタ画像になる）

MiseFits は Canvas を jsPDF で出していたのでこの問題が無かった。**MenuFits 固有の障壁。**

### 障壁2：書体が Google Fonts 依存で同梱できない

MiseFits のアプリ化は JS ライブラリ4本をローカル同梱すれば済んだが、MenuFits は
**39ファミリーの Web フォント**を読み込んでいる。日本語書体21種の同梱は容量的に非現実的
（数十〜百MB規模）。結果、アプリは実質オンライン必須になる。

### 障壁3：App Store 審査ガイドライン 4.2（Minimum Functionality）

単一 HTML を WebView で包んだだけのアプリはリジェクトされやすい。通すには写真ライブラリ連携・
共有シート・ネイティブ印刷・ファイル保存といった「アプリらしさ」を足す必要がある。

### 加えて：課金が二重管理になる

デジタルコンテンツの解放は Apple IAP が必須（Small Business Program で15%。Stripe の 3.6% より高い）。
**Web の買い切りと iOS の IAP は統合できず別売りになる。**
「Webで買ったのにアプリで使えない」という問い合わせの窓口対応まで引き受けることになる。

### やるとしたら

Web版 Pro が売れて需要が確認できてから、①障壁1の解決（ネイティブ印刷ブリッジ＋11デザイン全部での
出力実測）→ ②`Misefits/mobile/` の Expo + WebView 構成を流用 → ③RevenueCat で別売り → ④申請。
**3〜4週間規模。**

---

## 7. 実行計画

| フェーズ | 内容 | 目安 |
|---|---|---|
| **0. 販売の土台** | GA4導入／`privacy.html`・`tokushoho.html`／`pro.html`・`pro-unlock.html`・`faq.html`・`releases.html`（→ **2026-09-01 実装済み**。残りは §7-1 の3点） | 3〜4日 |
| **1. 決済基盤** | `misefits` プロジェクトへ MenuFits 用 Functions 3本を追加／Firestore コレクション／Stripe **テストモード**で通し確認（`4242 4242 4242 4242`）／解放UI | 3〜4日 |
| **2. ロック実装** | §3のチェックリスト13箇所に `isPro()` 分岐／PROバッジ／購入前モーダル／GA4イベント | 4〜6日 |
| **3. 本番切替** | 本番の商品・Price ID・Payment Link・Webhook を作成、`sk_live_` と本番Webhookシークレットを設定して再デプロイ | 1日 |
| **4. 告知** | **ここで初めて** note記事を公開。記事の「料金について」の節は書き直す（現在の下書きは「すべての機能を無料で」となっている） | 1日 |
| **5. 観測** | `pro_lock_hit` の `feature` 内訳と購入率を見る。**ここで初めて次を決める** | 1〜2ヶ月 |
| **6.（条件付き）** | Pro⑥の出力拡張（用紙サイズ・画像書き出し・一括価格改定・入稿PDF）／Proデザイン追加／iOSアプリ／サブスク | 実績次第 |

**フェーズ0〜4で 12〜16日。** 新機能を1つも作らないので、前案（15〜25日＋基盤）より大幅に短い。

**順序の厳守**：フェーズ3（有料化）が終わるまでフェーズ4（告知）に進まない。逆にすると、
公開ページで「無料」と言った直後に有料化する形になり、いちばん避けたい落差を自分で作ることになる。

### 7-1. フェーズ0 の実装記録（2026-09-01）

**作成したファイル**

| ファイル | 内容 |
|---|---|
| `page.css` | サブページ6枚の共通スタイル。MenuFits の配色（生成り・墨・金）と書体に合わせた |
| `analytics.js` | GA4ローダー＋`trackEvent()` ラッパー。**測定IDは未設定（空）** |
| `privacy.html` | プライバシー・免責。Google Fonts／GA4／Stripe の3つの外部通信を明記。オプトアウトUI付き |
| `tokushoho.html` | 特定商取引法に基づく表記。MiseFits の記載を MenuFits 用に調整 |
| `pro.html` | 料金・Proガイド。無料/Pro の比較表、Pro 6項目の説明、購入導線3か所、ライセンス条件、FAQ |
| `pro-unlock.html` | 決済後のリダイレクト先。`menufitsIssueLicense` を1.5秒間隔・最大約12秒ポーリング |
| `faq.html` | よくある質問。`FAQPage` の構造化データ入り |
| `releases.html` | 更新履歴（2026-08-21 / 08-22 / 09-01）。Pro リリース時の追記用テンプレートをHTMLコメントで用意 |
| `.claude/launch.json` | ローカル確認用の静的サーバー設定（`python -m http.server 8123`） |

**`index.html` に入れた変更**（いずれも Pro に触れないので、いま出しても安全）

- GA4ローダーをインラインで追加（`analytics.js` と同内容。**測定IDは未設定**）
- フッターの「プライバシー・免責」をモーダルから `privacy.html` へのリンクに変更。
  モーダル版は削除した（オプトアウトUIと決済の記載が必要になり、内容が二重管理になるため）
- ヘルプの「和文15種 × 欧文8種」を実際の **「和文21種 × 欧文18種」** に修正

**GA4（2026-09-01 作成・設定済み）**

`studio@kokokikaku.com` では既存の「Default Account for Firebase」にプロパティを作成できなかった
（`kokokikaku-web` を含む `mikan@` 側のアカウントで、studio@ に編集者権限が無い）。
そのため**アナリティクスアカウントごと新規作成**した。

| 項目 | 値 |
|---|---|
| アナリティクスアカウント | **ここ企画**（新規作成。studio@ が所有） |
| プロパティ | **MenuFits**（タイムゾーン 日本 GMT+09:00 ／ 通貨 日本円 ¥） |
| データストリーム | MenuFits ／ `https://menufits.kokokikaku.com` ／ ストリームID `15536863957` |
| **測定ID** | **`G-4L5JYWNVGJ`** — `analytics.js` と `index.html` の2か所に設定済み |
| データ共有設定 | **4項目すべてオフ**（Googleのプロダクトとサービス／モデリング／テクニカルサポート／ビジネスの最適化案）。必要になれば管理画面で戻せる |
| 規約 | 日本の利用規約に同意。GDPRのデータ処理規約にも同意。Marketing Platform の Cookie は「No thanks」 |
| 業種・規模 | ビジネス、産業 ／ 小規模（1〜10名）。実態と違えば管理画面で変更可 |
| ビジネス目標 | 売上の促進 ／ ウェブ・アプリのトラフィックの分析 |

動作確認済み：タグが発火すること（`gtag` 定義・`dataLayer` 追加・タグスクリプト注入）、
および **`menufitsAnalyticsOptOut` を立てると本当に読み込まれない**こと
（`ga-disable-G-4L5JYWNVGJ` が立ち、`trackEvent` が no-op に戻る）を実測した。

> **MiseFits の GA4 プロパティ（`G-W0Y7GMPVRK`）は studio@ から見えない。**
> 別の Google アカウント側にある可能性がある。MenuFits には影響しないが、
> 移行の取りこぼしとして別途確認したほうがよい。

### 7-2. フェーズ2（ロック実装）の記録（2026-09-01）

**販売開始スイッチ `PRO_LAUNCHED`（重要）**

`index.html` の先頭に `const PRO_LAUNCHED=false;` を置いた。**false のあいだは無料／Pro の
切り分けが一切効かない**（`isPro()` が常に true を返し、PROバッジも Pro欄も出ない）。

これを入れた理由：**ロックだけ先に本番へ出すと「買えないのに機能が使えない」状態になり、
いまより明確に悪くなる。** `main` への push が即デプロイされる構成なので、ブランチを切る代わりに
コードは本番へ置いたまま眠らせ、決済が通るようになった時点で1行 true にする。
不具合が出たら false に戻すだけで即座に元の挙動へ戻せる（ロールバック手段も兼ねる）。

**ロックした13箇所**（すべて `proGate(feature)` 経由。`pro_lock_hit` を送る）

| 場所 | feature | 挙動 |
|---|---|---|
| サイドバーのデザイン | `design:<id>` | Pro 6種に PROバッジ。押すと購入前モーダル |
| LPのデザイン一覧 | `design:<id>` | 同上。ただし**「無料のデザインで始める」ボタンを併設**（まだ何も試していない人に買うか帰るかの二択を出さない） |
| 和文・欧文の書体 | `font_jp:` / `font_en:` | Pro の option に「🔒 PRO」。選ぶと**選択を元に戻して**モーダル |
| 背景パターン | `pattern:<id>` | 無料6種以外にバッジ＋ロック |
| 飾り枠 | `deco:<id>` | 無料3種以外にバッジ＋ロック |
| 詳細パレット（紙・文字・柄・枠） | `color` | `pointerdown` で先に止める（color input は click では止まらない） |
| 背景画像 | `bgimg` | ロック |
| 表紙・裏表紙／表紙スタイル | `cover` | **「外す」操作は常に通す**（Pro で付けた表紙を外せなくなると詰むため） |
| 3ページ目以降（頁+） | `page3` | 2ページ目までは通る |
| 自由配置ブロック追加 | `block` | ロック |
| テンプレート文字のツールバー | `fieldtool` | 無料版では中身を **`⠿ A± PRO` の1ボタン**に差し替え（存在は伝わるが、押すまで邪魔をしない） |
| スロット保存 | `slot` | 1枠目は通る。**既存枠への上書きは常に通す** |
| 品目ライブラリ ☆／📋 | `itemlib` | ロック |
| 店舗プロフィール 登録／適用 | `shopprofile` | ロック |

**ロックしていないもの（意図的）**：A4のPDF出力・印刷、自動保存、`.json` の書き出しと読み込み、
Undo/Redo、文字装飾、品目・ヘッダー・ロゴの写真、アクセント色。

**動作確認**（`PRO_LAUNCHED` を一時的に true にして実測）
- 13箇所すべてでモーダルが出て、状態が変わらないこと（デザインは元のまま、書体は選択が戻る）
- 無料側の操作（無料デザイン・無料書体・2ページ目への送り・1枠目のスロット保存）は素通りすること
- **PDF出力は無料でも必ず通る**こと
- ライセンスを立てると、バッジと「🔒 PRO」が消え、Pro欄が「有効です」に変わり、Proデザインが適用できること
- `PRO_LAUNCHED=false` に戻すと、バッジ0・Pro欄非表示・Proデザインも素通り＝**従来と同一の挙動**
- コンソールエラーなし

### 7-3. フェーズ1（決済基盤）の記録（2026-09-01）

**Functions のコード（`Misefits/functions/menufits.js` に新規作成）**

`index.js` の MiseFits 用3関数には**一切触れていない**。末尾に
`Object.assign(exports, require("./menufits"));` を1行足しただけ。

| 項目 | 値 |
|---|---|
| 関数 | `menufitsStripeWebhook` / `menufitsIssueLicense` / `menufitsVerifyLicense` |
| Firestore | `menufitsLicenses` / `menufitsSessions` |
| キー形式 | `MNPRO-XXXX-XXXX-XXXX` |
| CORS | `https://menufits.kokokikaku.com` のみ |
| デバイス上限 | 5台 |

**Stripe（サンドボックス／テストモードで作成済み）**

| 項目 | 値 |
|---|---|
| Stripeアカウント | `acct_1U9FTFKwklnRvDMq`（**ここ企画サンドボックス**） |
| 商品 | MenuFits Pro `prod_VBA2wAN390xnot` ／ ¥1,480 JPY・1回限り |
| Price ID | `price_1UAniYKwklnRvDMqKN1SK2ob`（`functions/.env` に設定済み） |
| Payment Link | `https://buy.stripe.com/test_7sYaEY6NY86Y1cxcn70VO01`（`plink_1UAnkaKwklnRvDMq68PpQPYg`） |
| リダイレクト先 | `https://menufits.kokokikaku.com/pro-unlock.html?session_id={CHECKOUT_SESSION_ID}`（DOMから実値を照合済み） |
| Managed Payments | **オフ**（既定はオンで、取引あたり3.5%の追加手数料がかかる） |
| 電話番号の収集 | オフ |
| Webhook | `we_1UAnlzKwklnRvDMquCZwr28t` ／ 宛先 `.../menufitsStripeWebhook` ／ `checkout.session.completed` の1件のみ |

> **サンドボックスはライブとは別アカウントだった。** `functions/.env` の MiseFits 用
> `STRIPE_PRICE_ID` は `price_1U9KMg3C6tqke0fyB4wtmO3T`（ライブ側の別アカウント）で、
> 今回作った ID は `…KwklnRvDMq`（サンドボックス）。**ライブ側の販売には一切影響しない。**

**そのため `STRIPE_SECRET_KEY` を共有しない設計にした。**
共有シークレットはライブ用の `sk_live_` なので、サンドボックスのセッションを読めない
（Price ID の照合で `listLineItems` を呼ぶため必ず失敗する）。MenuFits には
`STRIPE_SECRET_KEY_MENUFITS` を別に持たせ、検証中はサンドボックスの鍵、販売開始時に
ライブの鍵を入れ直す。片方の鍵を差し替えても、もう片方の販売を巻き込まない。

> **Price ID の照合は「念のため」ではなく必須。** Stripe の Webhook は、その
> イベント種別を購読している**すべてのエンドポイントに配信される**。同じアカウントで
> 2商品を売る段階になると、MiseFits の購入で MenuFits のキーが発行されうる。
> 両側とも Price ID が設定されていることを、販売開始前に必ず確認すること。

**まだ残っている作業**

1. **シークレットを2つ登録する**（値を扱うので、こちらでは実行していない）。
   署名シークレットは Stripe の Webhook 詳細ページ「署名シークレット」から取得する。

   ```bash
   cd C:/Users/chaha/repos/Misefits
   firebase functions:secrets:set STRIPE_SECRET_KEY_MENUFITS      # サンドボックスの sk_test_...
   firebase functions:secrets:set STRIPE_WEBHOOK_SECRET_MENUFITS  # we_1UAnlz... の whsec_...
   ```

2. **関数3本だけをデプロイする**（既存の MiseFits 3本には触れない）。

   ```bash
   firebase deploy --only functions:menufitsStripeWebhook,functions:menufitsIssueLicense,functions:menufitsVerifyLicense
   ```

   ※ 新しい関数は Cloud Run サービスが新規に作られるため、`allUsers` への公開が
   組織ポリシーでブロックされる可能性がある（§5-2 の(3)と同じ罠）。
   403 が返るときはそれを疑う。Windows では `firebase` が
   `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` で異常終了することがあるので、
   **Cloud Run のサービス一覧で実際の状態を確認する**こと。

3. **通し確認**：`PRO_PURCHASE_URL` にサンドボックスの Payment Link を入れて
   テストカード `4242 4242 4242 4242` で決済 → Webhook 発火 → Firestore にキー発行 →
   `pro-unlock.html` でキー表示 → アプリで解放、まで一気通貫で確認する。
   **確認が済んだら `PRO_PURCHASE_URL` は空に戻す**（サンドボックスのリンクを公開しない）。

4. **`index.html` の Pro 文言・JSON-LD の書き換えと `sitemap.xml` への追加**（§4-2 のとおり**フェーズ3で**）

5. **`PRO_LAUNCHED` を true にする**（本番の Payment Link 設定と同時。実質的な「販売開始」ボタン）

6. **ヘルプ（`#helpBtn`）の各項目に PRO 表記を足す。** いまは付けていない。
   販売前に付けるとロックされていない機能に PRO と書くことになり混乱するため、
   **フェーズ3で `PRO_LAUNCHED` と同時に**入れる。対象は 5（品目ライブラリ・店舗プロフィール）、
   7（表紙・裏表紙）、8（自由配置ブロック・テンプレート文字の移動）、11（スロット保存）、12（書体・素材）

7. **本番モードへの切り替え**：ライブアカウントで商品・Price ID・Payment Link・Webhook を作り直し、
   `functions/.env` の `STRIPE_PRICE_ID_MENUFITS` とシークレット2つをライブ用に差し替えて再デプロイ。
   **テストと本番で署名シークレットは別物**なので取り違えないこと。

**公開の判断**：上の `index.html` の3変更と `privacy.html` は、Pro に触れていないので
先に本番へ出してよい。むしろ **GA4 を先に入れておくと、有料化の前に「そもそも人が来ているのか」
が分かる**（現在それを知る手段が無い）。残り5ページはどこからもリンクせず、
`sitemap.xml` にも入れないまま置いておけば、実質見つからない。

---

## 8. 確定した判断と、残る未決事項

### 確定（2026-09-01）

| 項目 | 決定 |
|---|---|
| 価格 | **買い切り ¥1,480**。MiseFits と揃える。姉妹ツールで価格が違うと説明が増えるため |
| 無料デザイン | **5種**（EDITORIAL／KINARI／NOIR／SHU／ALBUM）。Pro 6種 |
| テンプレート文字のドラッグ・拡縮 | **Pro**。自由配置ブロックと合わせて「作り込み」バンドルの核にする |
| 課金モデル | 買い切り一本。月額・年額は採らない |
| iOSアプリ | Web版 Pro の実績が出るまで着手しない |

### 確定（2026-09-01・追加分）

| 項目 | 決定 |
|---|---|
| スロット保存 | **無料1枠**。0枠だと機能の存在自体が伝わらない。1枠あれば使い方が分かった上で2枚目で当たる |
| ページ上限 | **無料2ページまで**。1枚もの＝1ページ、表裏＝2ページ。3ページ目からが冊子 |
| 無料の書体 | 下表のとおり和文8×欧文6（48通り） |
| 無料のパターン・飾り枠 | パターン6種（「なし」含む）／飾り枠3種（「なし」含む） |

**無料の書体（`JPF` / `ENF` のキー）** — 系統が偏らないよう、各系統から最低1つずつ選んだ。

| | 無料8種 | Pro 追加13種 |
|---|---|---|
| 和文 | `shippori`（明朝）／`serifjp`（明朝・太）／`gothic`（ゴシック）／`noto`（ゴシック）／`maru`（丸ゴ）／`klee`（手書き風）／`yuji`（筆）／`mochiy`（ポップ） | `zenold` `kaisei` `hina` `rock` `reggae` `dot` `kiwi` `yusei` `zenat` `ibm` `mplus` `kure` `shipb1` |

| | 無料6種 | Pro 追加12種 |
|---|---|---|
| 欧文 | `cormorant`（セリフ）／`marcellus`（クラシック）／`playfair`（ディスプレイ）／`jost`（サンセリフ）／`space`（モダン）／`bebas`（極太コンデンス） | `fraunces` `italiana` `cinzel` `dmserif` `oswald` `poiret` `instrument` `syne` `young` `brico` `gloock` `vibes` |

各デザインの既定書体は CSS 側で決まっていて `state.fontJp` / `state.fontEn` は空文字なので、
**どれを Pro にしても初期表示は壊れない**（`JPF` / `ENF` は上書き指定のときだけ効く）。

### Pro の設定が入った `.json`・旧スロットを無料版で開いたときの扱い（確定）

**方針：読み込んだデータはそのまま表示・印刷できる。ただし Pro 要素を新たに選ぶ・変更する操作はロックする。**

- 読み込み時に `migrate()` で降格させない（**作ったものを壊さない**）
- Pro のデザイン・書体・パターン・自由配置ブロックを含むデータでも、表示と PDF 出力は通す
- Pro 要素を**変更しようとした瞬間**に購入前モーダルを出す（＝通常のロック地点と同じ挙動）
- 読み込み時に一度だけ「このデータには Pro の設定が含まれています」と知らせる

**理由**：ライセンス判定はクライアント側にしかないので、`.json` の受け渡しによる回避は
どうやっても完全には塞げない（devtools を開けば済む話でもある）。
**DRM 的な作り込みは、正直な利用者の体験だけを確実に損なう。**
¥1,480 の商品に対して割に合わない。抜け道を塞ぐことより、
「自分で作り込める」という Pro を買う理由を明確にするほうが実効性がある。

### 残る作業（判断ではなく実務）

- **`note-assets/リリースノート記事.md` の書き直し。** 現在の下書きには
  「すべての機能を無料でお使いいただけます」と書いてある。**公開前に必ず直す**

---

## 参照

- `C:\Users\chaha\repos\Misefits\HANDOFF.md` — 「課金設計」「Web版での買い切り販売」「GCP環境構築でハマった点」
- `C:\Users\chaha\repos\Misefits\AGENTS.md` — 「課金導線（Pro）の作り」「課金ファネルのイベント」
- `C:\Users\chaha\repos\Misefits\functions\index.js` — 流用元の実装
