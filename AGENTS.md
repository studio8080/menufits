# AGENTS.md — MenuFits

このリポジトリで作業する人・エージェント向けの指示書。まずこの1枚を読めば全体が分かるようにしている。
料金プランの設計と決済まわりの詳細は `PRO-PLAN.md` を参照。

## プロジェクト概要

飲食店向けのメニュー作成ツール。紙面の文字を直接クリックして打ちかえ、A4のPDFとして出力する。
サーバーにデータを送らず、すべてブラウザ内で完結する。

- 公開URL: https://menufits.kokokikaku.com/ （GitHub Pages / `main` / ルート）
- 実体は **単一の `index.html`**（バニラJS・ビルド工程なし）。LP とエディタが同一ページ
- 外部依存は Google Fonts（39ファミリー）のみ。**オンライン前提**
- 2026-09-01 から買い切りの「MenuFits Pro」（¥1,480税込）を販売中

## いちばん大事なルール

1. **`index.html` を直接編集する。** これが唯一の本番ソース。ビルド生成物ではない
2. **単一ファイル構成を維持する。** CSS も JS も `index.html` にインライン。
   静的サブページ用の `page.css` / `analytics.js` を `index.html` から読ませない
3. **`localStorage` は try/catch で包む。** プライベートモード等で例外になり得る
4. **破壊的・外部公開・課金を伴う操作は勝手に実行しない。** 特に `main` への push は
   即 GitHub Pages に反映される

### 二重に持っている値（片方だけ直すと壊れる）

| 値 | 場所 |
|---|---|
| GA4 測定ID `G-4L5JYWNVGJ` | `analytics.js` と `index.html` のインラインローダー |
| `PRO_PURCHASE_URL`（Stripe決済リンク） | `index.html` と `pro.html` |
| デザイン数・書体数などの訴求数値 | `index.html`（meta / JSON-LD / LP / ヘルプ）、`pro.html`、`faq.html`、`templates.html`、`llms.txt`、note記事 |
| 無料／Pro の切り分け（どの機能がどちら側か） | `index.html` の `FREE_*` 定数、`pro.html` の比較表、`templates.html` の無料/PROバッジ、業態別ページ本文 |
| フッターのリンク列 | 静的サブページ11枚と `index.html` のLPフッター |

### 販売開始スイッチ `PRO_LAUNCHED`

`index.html` の先頭にある。**`false` にすると無料/Proの切り分けが全て無効になり、
販売開始前の挙動に戻る。** 不具合時のロールバック手段。
`true` のあいだは `PRO_PURCHASE_URL` が設定されていることが前提。

## リポジトリ構成

```
index.html        アプリ本体（LP・エディタ・スタイル・スクリプト同梱）。ここを編集する
ogp-pro.png       pro.html 専用のOGP画像（価格が見える）。生成手順は「OGP画像」の節
samples/          サンプルPDF（A4）。ヘッドレスChromeで生成。生成手順は「サンプルPDF」の節
page.css          静的サブページの共通スタイル
analytics.js      GA4ローダーと trackEvent() ラッパー
ads.js            記事下の広告枠（アフィリエイト）。Pro購入者・オプトアウト中には出さない
templates.html    メニュー表テンプレート一覧（検索の受け皿の中心。11デザインへの入口）
menu-cafe.html    カフェのメニュー表の作り方
menu-izakaya.html 居酒屋の品書き・ドリンクメニューの作り方
menu-washoku.html 和食のお品書きの作り方（縦書き）
menu-chuka.html   中華・ラーメン店のメニューの作り方
guide.html        使い方ガイド（SEOの受け皿。アプリ内モーダルと内容をそろえること）
pro.html          料金・Proガイド（このページ単体で購入まで完結させる）
pro-unlock.html   決済後のリダイレクト先。ライセンスキー表示とワンクリック解放
faq.html          よくある質問（FAQPage 構造化データ入り）
releases.html     更新履歴
privacy.html      プライバシー・免責（解析のオプトアウトUI付き）
tokushoho.html    特定商取引法に基づく表記
404.html          カスタム404（トップへリダイレクト）
PRO-PLAN.md       料金プランの設計・決済基盤・踏んだ罠の記録
note-assets/      note記事の下書きとスクリーンショット（公開物ではない）
```

決済のバックエンドは **MiseFits リポジトリの `functions/menufits.js`**（Firebase プロジェクト
`misefits` に相乗り）。理由と手順は `PRO-PLAN.md` §5-2。

## セキュリティ上の注意

### 守れていること

- Firestore はクライアントからの直接読み書きを**全面拒否**。発行・検証は Cloud Functions のみ
- Functions の CORS は `menufits.kokokikaku.com` 固定。他オリジンはブラウザ側で弾かれる
- Webhook は Stripe の署名検証を通過したリクエストしか処理しない
- カード情報は Stripe が直接受け取り、当方は一切受け取らない
- 静的サブページ（12枚）には CSP を設定済み

### 気をつけるところ

- **`plain()` の戻り値を HTML 文字列に連結しない。** `plain()` はタグを剥がしたテキストを
  返すが、元が `&lt;img&gt;` のようにエスケープされていると `<img ...>` という**文字列**に
  なる。それを innerHTML に渡すと再度タグとして解釈される（二重デコード）。
  HTMLに差し込むときは **`escHTML()` を通す**。2026-09-01 に `openLibPicker` で実際に
  起きていたのを修正した
- **`sanitizeHTML()` は許可リスト方式**（`b` / `u` / `span`(color,background-color) のみ残す）。
  紙面の文字を innerHTML で描く箇所は必ずこれを通す。新しい描画箇所を足すときも同じ
- **ライセンス判定はクライアント側だけ。** 回避は完全には防げない前提の設計。
  DRM的な作り込みはしない（判断の経緯は `PRO-PLAN.md`）
- **`index.html` に CSP が無い。** サブページには入れたが本体は未設定。入れる場合は
  Google Fonts・インラインscript/style・`data:`/`blob:` 画像・cloudfunctions への
  `connect-src` を通す必要がある。**入れたら必ず全機能を通しで確認すること**（PDF出力・
  写真アップロード・ライセンス解放）

## SEO / LLMO

### いまの状態

- `robots.txt` は全許可（AIクローラーも明示的に許可）。`sitemap.xml` に12ページ
- `llms.txt` に概要・できること・料金・リンクを記載
- 構造化データ：`index.html` に `WebApplication`（無料と¥1,480の2つの `offers`）＋ `FAQPage`、
  `faq.html` に `FAQPage`
- GA4 稼働中（`G-4L5JYWNVGJ`）。課金ファネルのイベントは `PRO-PLAN.md` §4-3

### `guide.html`（2026-09-01 対応済み）

ヘルプがモーダルの中にしかなく、検索エンジンから見えない状態だった。
`guide.html` として静的ページに出した（本文約3,800字、`HowTo` + `FAQPage` +
`BreadcrumbList` の構造化データ入り）。

**モーダルは残してある**（その場で確認したい人のため）。**内容を直すときは
両方そろえること。** モーダル末尾からガイドへのリンクを置いてある。
ガイドはモーダルの丸写しではなく、つまずきやすい点（背景のグラフィック、
写真が出ないデザイン、縦書きのカンマ、データが消える条件）を厚くしてある。

### 集客ページ（2026-09-04 対応済み）

アプリ本体は索引対象のテキストが少ないため、検索の受け皿を別ページに出した。
MiseFits の `layout-*.html` と同じ分担。

- `templates.html` — **中心のページ。**「メニュー表 テンプレート 無料」を受ける。
  11デザインをカード表示し、`#d-<デザインID>` でアプリの該当デザインへ直接送る
- `menu-cafe.html` / `menu-izakaya.html` / `menu-washoku.html` / `menu-chuka.html`

構成は共通で「ヒーロー → 目次 → 考え方 → 具体例 → MenuFitsでの手順 → つまずきやすいところ →
FAQ → 関連ページ → CTA」。**中身の薄いページを量産しない。**1ページ2,500〜3,500字で、
その業態でしか書けないこと（決める順番・つまずき方）を必ず入れる。

**業態を増やすときは、この5枚のどれかをまるごとコピーして中身を差し替える。**
head（CSP・canonical・OGP・JSON-LD）とフッターはそのまま使える。差し替えるのは
`<title>` / `description` / `canonical` / OGP / JSON-LD の3つ（Article・FAQPage・
BreadcrumbList）/ 本文 / パンくず / 関連ページのリンク。
増やしたら **`sitemap.xml`・`llms.txt`・各ページの「関連ページ」・`index.html` のLP
（`.lplinks` のセクション）** にも足すこと。

候補（未実装）：`menu-price.html`（値上げと価格の見せ方）、ビストロ・フレンチ、
定食・食堂、喫茶・ベーカリー、テイクアウト・弁当。

### `#d-<デザインID>` のディープリンク

`index.html` の起動処理にある。`./#d-kinari` のように書くと、そのデザインで
近いサンプルが開く。Proのデザインを指した場合は、**無料のデザインで開いたうえで
購入モーダルを出す**（「押したのに何も起きない」を避ける）。
`templates.html` のカードがこれを使っている。デザインIDは `DESIGNS` 配列を見ること。

### LPの料金セクション（2026-09-04 対応済み）

`index.html` の「11のデザイン」の直後に `#lpPricing`（無料/Proの2カラム）を置いた。
**PROバッジを見た直後に「Proとは何か」を答える位置**にしてある。
`syncProUI()` が `PRO_LAUNCHED=false` のときに丸ごと隠す（売っていないものの価格を出さない）。
**無料の範囲の数値（5種・48通り・2ページ）を変えたら、ここと `pro.html` の比較表の両方を直すこと。**

### OGP画像

- `ogp.png` … 全ページ共通
- `ogp-pro.png` … `pro.html` 専用。価格（¥0 / ¥1,480）が見える

`ogp-pro.png` は Pillow で生成した。配色は `ogp.png` から実測（背景 `#f4f0e7` /
文字 `#26231c` / 金 `#c9a227`）。書体は游明朝Demibold（見出し）・游ゴシックMedium（本文）・
Century Gothic（欧文ロゴ）で、サイトの Shippori Mincho / Zen Kaku Gothic New / Jost の代替。
**価格を変えたら作り直すこと。**

### サンプルPDF

`samples/menu-{cafe,washoku,bistro,chuka}-sample.pdf`。すべて無料デザイン・A4・1ページ。
`templates.html` の「仕上がりを見る」から辿れる。`sitemap.xml` にも入れてある。

生成はヘッドレスChromeで、**アプリの印刷と同じ経路**を通す（別途PDFを作らない）。
`@page{size:A4;margin:0}` と `print-color-adjust:exact` が効くので、背景も出る。

```bash
# ローカルサーバーを起動しておく（python -m http.server 8123）
CHROME="/c/Program Files/Google/Chrome/Application/chrome.exe"
for s in cafe washoku bistro chuka; do
  PROF=$(mktemp -d)
  "$CHROME" --headless=new --disable-gpu --no-sandbox --user-data-dir="$PROF"     --no-pdf-header-footer --virtual-time-budget=45000     --print-to-pdf="$(cygpath -w "$PWD/samples/menu-$s-sample.pdf")"     "http://localhost:8123/#$s"
  rm -rf "$PROF"
done
```

**デザインやサンプルの内容を変えたら作り直すこと。**
生成後は必ず**目視で確認する**。ヘッドレスChromeはPDFビューアを描画しないので、
スクリーンショットでは中身を確認できない（PDFの内部構造だけでは判断を誤る。
2026-09-04 に、フォント辞書がオブジェクトストリームに入っていたせいで
「日本語フォントが欠けている」と誤読した）。

### 次に効きそうな施策（未実装・優先度順）

1. **業態別ページ専用のOGP画像** — いまは全ページ共通の `ogp.png`
2. **業態を増やす** — ビストロ／定食／喫茶・ベーカリー／テイクアウト。反応を見てから足す

## 広告（アフィリエイト）

`ads.js` が、静的ページの**記事下**に置いた `<aside class="adbox" data-ad hidden>` を処理する。
枠があるのは `guide.html` / `faq.html` / `templates.html` / `menu-*.html` の7枚。
**アプリ本体（`index.html`）には出さない。**

`releases.html` にも一度置いたが外した。更新履歴は検索流入がほぼ無く、読むのは既存利用者
（Pro購入者を含む）なので、広告を出す価値がない。`pro.html` / `pro-unlock.html` は購入導線と
競合するため、`privacy.html` / `tokushoho.html` は法定・規約のページなので、いずれも置かない。

### いま出しているもの（2026-09-04 掲載開始）

**もしもアフィリエイト「プリントアース｜ネット印刷通販の申込・購入」のテキスト広告を7枚すべてに**。
成果7%（購入）・審査なし・再訪問90日。**広告ID 95835（テキスト）**。
アンカー文言は「ネット印刷のプリントアース」——広告主名と業種だけを書き、こちらの評価は書かない。

バナー（広告ID 95902 / 300×250）も一度貼ったが**外した**。絵柄が
「最大10万円分のクーポンプレゼント!!」の原色で、紙面の落ち着きを壊すため。
`page.css` の `.adbox .ad-slot a` はこのテキスト広告用のスタイル。

**もしもが発行したコードから1点だけ変えてある。** `//af.moshimo.com` のような
プロトコル相対URLを `https://` に固定した（3か所 × 7枚）。追跡パラメータ
（`a_id` / `p_id` / `pc_id` / `pl_id`）は一切触っていない。理由は2つ:

- 各ページの CSP が `img-src ... https://*.moshimo.com` なので、http に降格すると**画像が黙って消える**
- ローカル（`http://localhost:8123`）で確認できない

**コードを差し替えるときも同じ処理をすること。** そのままだとローカルで
CSP エラーになり、動作確認ができない。

### 何を出すか（方針・2026-09-04 決定）

**メニューを作り終えた人には「刷る」という次の行動が必ずある。** 広告が邪魔にならないのはここだけ。

| 枠 | 出すもの |
|---|---|
| `templates.html` / `guide.html` / `faq.html` / `menu-izakaya` / `menu-chuka` | ネット印刷（ラクスル／プリントパック／グラフィック） |
| `menu-cafe.html` | ネット印刷。余力があればラミネーター（本文のFAQで触れている） |
| `menu-washoku.html` | 上質紙・和紙風の用紙（本文「上質紙に刷ると格が上がる」を受ける） |

**POSレジ・会計ソフト・求人媒体・事業用カードは入れない。** 単価は高いが、
「メニューを1枚作りに来ただけの人」には押し売りに見える。¥1,480 の Pro を売っている
サイトなので、広告で信頼を削るほうが損になる。各ページの `<aside>` のコメントに、
そのページに置くものを書いてある。

### ASP と提携状況（2026-09-04 時点）

**もしもアフィリエイト**（ログインID `totonou_note`）

| 項目 | 状態 |
|---|---|
| メディア「MenuFits（メニューフィッツ）」 | **審査中**。カテゴリ＝スモールビジネス、月間PV＝0から100（未確認の既定値。`編集`から直せる） |
| プリントアース（ネット印刷） | **提携中**（審査なしのため即時） |
| 楽天市場の商品購入 | **提携中**（メディア登録時の「おすすめプロモーション」で即時提携）。まだ掲載していない |
| Amazon | 申請していない（本人の判断） |

もしもにネット印刷はプリントアース1件しか無い。ラクスル／プリントパック／グラフィックは無い。

**A8.net**（メディアID `a26072638566`）

| 項目 | 状態 |
|---|---|
| サイト「MenuFits（メニューフィッツ）」 | **登録済み（サイトID 006）**。カテゴリ＝飲食・グルメ、開設日 2026/08、月間訪問者・PV とも「100以下」 |
| ラクスル（ネット印刷） | **申請中**（審査あり） |

**A8 は「サイト情報の登録・修正」にログインID＋パスワードの再認証を求める。**
エージェントからは実行できない。**再認証だけは本人が通すこと。**
通したあとはセッションが共有されるので、フォーム入力から登録完了までは
エージェント側で進められる（2026-09-04 はその手順で登録した）。

**新管理画面（`media-console.a8.net`）はSPAで、`element.value` への代入では
フォームの状態が更新されない。** ネイティブsetter＋`input`イベントで入れること。
紹介文には文字数制限がある（211字で「多すぎます」、99字で通過）。

狙うプログラム：**「テレビCMでもおなじみの注目ベンチャー！激安ネット印刷の【ラクスル】」**
（プログラムID `s00000013809001`）。注文（税抜）**5%**・**審査あり**・再訪問90日［初回のみ］・
成果確定目安 約30日・EPC 13.06・確定率 92.44%。カテゴリは「写真・プリント」。
成果対象は `raksul.com` / `design.raksul.com` / `dm.raksul.com` / `am.raksul.com` からの購入で、
入金・データ入稿のうえ21日以内に出荷完了したもの。

> 料率だけならプリントアース（7%・審査なし）が上。ラクスルは知名度で押せるぶんクリック率が期待できる。

**ラクスル側の掲載条件（守らないと提携解除される）**

- **広告と分かる表示が必須。** 一般消費者が認識できる位置にわかりやすく。
  → `ads.js` が「広告」ラベルを自動で付けているので満たしている。**このラベルを消さないこと**
- **公開後、「広告掲載URL管理」から掲載ページのURLを提出する**（審査通過後の宿題）
- リスティング広告のNGキーワードあり（「ラクスル」「raksul」等）。当サイトは出稿しないので無関係
- ステマ規制（令和5年内閣府告示第19号）に触れる表示をすると予告なく提携解除

**A8 は「サイト情報の登録・修正」にログインID＋パスワードの再認証が要る。**
エージェントからは実行できないので、**MenuFits のサイト登録は本人が行うこと。**
登録が済めば、そのあとの提携申請と広告コードの取得・貼り付けはエージェント側でできる。

### まだ手を付けていないこと

- **ラクスルの提携審査の結果待ち。** 通ったら広告コードを取得して掲載する。
  プリントアースと差し替えるか併用するかは、そのとき料率と見え方を見て決める
- **審査通過・公開後に、A8の「広告掲載URL管理」へ掲載ページURLを提出する**
- **もしものメディア審査の結果待ち。** 否認されるとリンクが機能しなくなる

### 楽天の商品リンクは出さない（2026-09-04 判断）

提携はしてあるが掲載しない。理由は3つ。

1. かんたんリンクは商品画像＋ショップボタンの重いカード。**バナーをやめてテキストにした判断と逆行する**
2. 記事下は「1か所だけ」の設計なので、置くならプリントアース（7%）を
   **楽天の用紙（実質2〜4%×数百円）に置き換える**ことになり、関連性でも収益でも悪化する
3. 1ページに広告主を2つ並べると、¥1,480 の Pro を売っているサイトとして雑に見える

提携自体は残してある（将来 `menu-washoku` の上質紙などで使う余地はある）。

### 出さない条件（どれか1つでも当てはまれば DOM ごと消す）

1. **Pro購入済み**（`localStorage` の `menufitsWebLicense`）
2. `<template>` の中が空（＝広告コード未設定）。いまはこの状態なので**枠は表示されない**
3. 解析オプトアウト中（`menufitsAnalyticsOptOut`）／Do Not Track

広告コードは `<template>` の中に置き、表示条件を満たしたときだけ DOM へ差し込む。
**出さない相手には画像リクエストすら発生しない**（インプレッションも立たない）。

### 広告コードを貼るときの手順

1. もしもアフィリエイト／A8.net で、**バナーまたはテキスト（`<a>` と `<img>` だけ）**の
   コードを取得する。**`document.write` を使う形式は動かない**（読み込み後に差し込むため）
2. 各ページの `<template></template>` の中に貼る
3. そのページの CSP に配信元が入っているか確認する。既定で
   `img-src` に `*.a8.net` / `*.moshimo.com` / `*.rakuten.co.jp` / `m.media-amazon.com`、
   `frame-src` に `*.a8.net` / `*.moshimo.com` を通してある
4. 貼ったあと、**Pro解放済みのブラウザで枠が消えることを確認する**

### 「広告」の表示は2か所（消さないこと）

ステマ規制（景品表示法／令和5年内閣府告示第19号）と、広告主の掲載条件への対応。
**ラクスルは「ファーストビュー等、一般消費者が認識できる位置に広告と分かる表示」を
求めており、無い場合は提携を承認しない／解除しうる。** そのため2か所に出している。

| 場所 | 出るもの | 実装 |
|---|---|---|
| パンくずの直後（ファーストビュー） | 「このページには広告（アフィリエイトリンク）が含まれます。広告について」 | `[data-ad-note]` を `ads.js` が表示 |
| 記事下の広告枠のすぐ上 | 「広告」 | `.ad-lbl` を `ads.js` が生成 |

**広告を出すときだけ、両方を同時に出す。** 広告コードが空のときや、Pro購入者・
オプトアウト中の人には**どちらも消える**（広告が無いのに「広告が含まれます」と
書いてある状態を作らない）。広告枠を足すページには、冒頭の `[data-ad-note]` も
セットで置くこと。
`privacy.html` の「外部への通信 4. 広告（アフィリエイト）」も実態と合わせること。
**ASP名を変えたらそこも直す**（現在は「もしもアフィリエイト（株式会社もしも）」を名指しし、
同社のプライバシーポリシーへリンクしている）。

**リリースノート（`releases.html`）には広告の話を書かない。**

### LLMO（AIからの参照）で気をつけること

`llms.txt` は**料金を含めて実態と一致させる**。AIが「MenuFitsは完全無料」と答える状態は
いちばん困る。数値（デザイン数・書体数）を変えたら `llms.txt` も必ず直す。

## テスト観点（毎回確認）

- サンプル読み込み → 文字の打ちかえ → PDF/印刷（背景のグラフィックにチェック）
- デザイン切替（無料5種はそのまま、Pro 6種は購入前モーダル）
- 書体・パターン・飾り枠・色・表紙・3ページ目・自由配置ブロック・スロット2枠目・
  品目ライブラリ・店舗プロフィールで、それぞれロックが出る
- **PDF出力は無料でも必ず通ること**（ここをロックしたら無料版の意味が無い）
- ライセンスキーを入れて解放 → バッジと「🔒 PRO」が消える
- `PRO_LAUNCHED=false` に戻すと、バッジ0・Pro欄非表示・全機能開放（従来の挙動）
- スマホ幅（≤860px）でドロワー・紙面が先頭
- ブラウザコンソールに JS エラーが出ていない
- `templates.html` のカードを押すと、そのデザインでアプリが開く
  （PROのカードは無料デザインで開いたうえで購入モーダルが出る）
- 広告コードを貼ったときは、**Pro解放済みのブラウザで枠が消える**こと

ローカル確認はサブページが相対パスで `page.css` を読むため、簡易サーバーが必要。

```bash
python -m http.server 8123
```
