# 収益導線の計測補完 — 2026-09-23

## 今回の実装（公開済み）

GA4・Search Consoleと既存ASPレポートを確認し、無料利用の到達点が計測できない問題を先に修正した。

- `menu_edit_start`: 紙面の文字を初めて変更したとき、ページ読み込みごとに1回。店名・本文・価格など入力値は送らない。
- `menu_print_request`: 「印刷画面をひらく」のクリック。PDF保存成功・印刷完了とは扱わない。
- `affiliate_click`: 既存の記事広告クリック。provider（a8/moshimo）とplacement（article）のみ付与。URLやパラメーターは付与しない。遷移は妨げない。
- 既存の解析オプトアウト・DNT・購入済み広告非表示は維持。

広告追加、価格変更、購入条件変更、新規契約はしていない。

## 判断根拠（非公開運用メモ）

9月23日収集のGA4スナップショット（8月26日〜9月22日）ではMenuFitsは17セッション、有料機能選択イベント9回。購入ボタンクリックは取得イベント一覧に見当たらない。イベント数は人数ではなく、編集・印刷を計測していなかったため無料利用からの移行率は算出できない。GA4の購入記録の有無だけで実売上は断定しない。

Search Consoleは取得遅延のある別期間（8月24日〜9月19日）。MenuFitsは8表示・0クリック。露出が小さく、価格やCTAを大きく変更する根拠には不足する。既存の業態別記事とテンプレート導線を育てる方針。

もしもは9月1日〜22日でMenuFits 15表示・0クリック・発生0。A8サイト別の同期間集計にはMenuFits行がない。行がないことを未登録や提携切れとは判定しない。ASPとGA4では計測対象が異なるため単純な割り算でCVRを作らない。

## 次の評価

公開後に同期間で、検索流入・紙面編集・印刷要求・有料機能選択・購入リンククリック・決済側売上を照合する。GA4ユーザー単位のファネルで確認し、イベント件数の単純比を利用者転換率として扱わない。

## 検証

`node --test tests/purchase-tracking.test.cjs tests/funnel-tracking.test.cjs`: 16/16成功。

初回編集の重複防止、入力値非送信、印刷処理維持、解析拒否時の未送信、広告プロバイダー判定、偽装ホスト除外、Pro広告非表示、計測例外時の安全性、既存購入計測を確認。

計測改善は d73f5b6 で公開ジョブ成功。記事導線・広告案内は 2a7eb65 で公開依頼済み。既存の購入計測修正とは別の変更。

